{
  "name": "example",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ../ && doric build && cd - && doric build",
    "dev": "doric dev",
    "clean": "doric clean",
    "android": "doric run android",
    "ios": "doric run iOS"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@types/varint": "^6.0.0",
    "doric-websocket": "file:../",
    "reflect-metadata": "^0.1.13",
    "rollup": "^2.56.2",
    "typescript": "^4.3.5",
    "varint": "6.0.0"
  },
  "optionalDependencies": {
    "ios-deploy": "^1.11.4"
  }
}
